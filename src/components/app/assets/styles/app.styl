@import "../../../../assets/styles/_index.styl"

.main
  padding-top: 58px

html, body, .main
  min-height 100%
  height: 100%

.main
  position: relative
  z-index: 0
  transition all .5s
  transform: translate(0)
  right: 0
  &__open__left
    transform: translate(300px)